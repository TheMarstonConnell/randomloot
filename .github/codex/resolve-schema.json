{
  "type": "object",
  "additionalProperties": false,
  "required": ["threads"],
  "properties": {
    "threads": {
      "type": "array",
      "description": "One entry per thread in threads.json, in any order.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "solved", "reason"],
        "properties": {
          "id": {
            "type": "string",
            "description": "The thread id, copied exactly from threads.json."
          },
          "solved": {
            "type": "boolean",
            "description": "true only when the complaint no longer holds in the current tree."
          },
          "reason": {
            "type": "string",
            "description": "One short sentence of justification, for the workflow log."
          }
        }
      }
    }
  }
}
